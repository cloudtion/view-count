# Firebase Configuration (for local dev server only)
# Get these from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Note: These are only needed for local development. Firebase Functions auto-configures these.
FB_PROJECT_ID=your-project-id
FB_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FB_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Optional: Firestore collection name (default: view-counts)
FB_COLLECTION_NAME=view-counts

# Optional: Cache TTL in milliseconds (default: 1800000 = 30 minutes)
# Views from the same visitor within this window won't increment the count
CACHE_TTL_MS=1800000

# Optional: Dev server port
SERVER_PORT=3000

# Optional: Log source URLs (referer/fallback-id) to Firestore
SOURCE_LOGGING=false
