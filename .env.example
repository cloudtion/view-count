# Firebase Configuration
# Get these from Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Optional: Firestore collection name (default: view-counts)
FIREBASE_COLLECTION_NAME=view-counts

# Optional: Cache TTL in milliseconds (default: 1800000 = 30 minutes)
# Views from the same visitor within this window won't increment the count
CACHE_TTL_MS=1800000

# Optional: Dev server port
PORT=3000

# Optional: Custom styling
STYLE_BG_COLOR=#1a1a1a
STYLE_TEXT_COLOR=#ffffff
